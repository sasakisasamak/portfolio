# Portfolio Site

Next.js + TypeScript + Tailwind CSS で構築したポートフォリオサイトです。

## セットアップ

```bash
npm install
cp .env.local.example .env.local  # 値を設定
npm run dev
```

## 環境変数

`.env.local.example` を参照してください。お問い合わせフォームの送信には [Resend](https://resend.com) の API キーが必要です。
